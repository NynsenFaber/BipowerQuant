# Compiled C++ extensions
*.so
*.dylib
*.pyd

# CMake build directory and artifacts
build/
compile_commands.json

# Python virtual environments and caches
.venv/
__pycache__/
*.pyc

# Test, coverage and lint artefacts
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
junit*.xml

# Editor state
.vscode/

# Local data, caches and generated results
*.csv
*.parquet
*.npz
# Scored metrics and walk-forward results: regenerable, and the figures that read
# them are committed. `git add -f` one if you want a specific run to stay pinned.
data/*.json
# Run transcripts. Same reasoning: a log is a record of one machine's afternoon.
*.log

# Trained model checkpoints (git add -f the ones worth keeping)
weights/*.pt

# Jupyter
.ipynb_checkpoints/

.DS_Store

# Private working notes — run history, audit trail, next steps
TODO.md
NOTE.md